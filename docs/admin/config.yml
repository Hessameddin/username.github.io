backend:
  name: github
  repo: username/username.github.io  # نام کاربری خودت
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  # تراکنش‌های واریز سکه
  - name: "transactions"
    label: "💸 تراکنش‌های واریز"
    folder: "docs/_transactions"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{user_id}}"
    fields:
      - {label: "تاریخ", name: "date", widget: "datetime", format: "YYYY-MM-DD HH:mm"}
      - {label: "شناسه کاربری", name: "user_id", widget: "string", required: true}
      - {label: "نام کاربر", name: "username", widget: "string", required: false}
      - label: "پکیج سکه"
        name: "coin_package"
        widget: "select"
        options: ["1000", "3000", "5000", "10000", "20000"]
        default: "1000"
      - {label: "تعداد سکه", name: "coin_amount", widget: "number", value_type: "int", default: 1000}
      - {label: "مبلغ (تومان)", name: "amount", widget: "number", value_type: "int", default: 50000}
      - label: "وضعیت"
        name: "status"
        widget: "select"
        options: ["در انتظار", "پرداخت شده", "واریز شده", "لغو شده"]
        default: "در انتظار"
      - {label: "شماره تراکنش", name: "transaction_id", widget: "string", required: false}
      - {label: "توضیحات", name: "notes", widget: "text", required: false}
      - {label: "تاریخ واریز", name: "delivery_date", widget: "datetime", required: false}

  # کاربران ثابت
  - name: "users"
    label: "👥 کاربران ثابت"
    folder: "docs/_users"
    create: true
    slug: "{{user_id}}"
    fields:
      - {label: "شناسه کاربری", name: "user_id", widget: "string", required: true}
      - {label: "نام", name: "name", widget: "string", required: false}
      - {label: "شماره تماس", name: "phone", widget: "string", required: false}
      - {label: "تلگرام", name: "telegram", widget: "string", required: false}
      - {label: "تاریخ ثبت", name: "register_date", widget: "datetime", default: "2024-01-01"}
      - {label: "اعتبار (تومان)", name: "credit", widget: "number", default: 0}
      - {label: "توضیحات", name: "notes", widget: "text", required: false}

  # پکیج‌های سکه
  - name: "packages"
    label: "💰 پکیج‌های سکه"
    folder: "docs/_packages"
    create: true
    slug: "{{title}}"
    fields:
      - {label: "عنوان", name: "title", widget: "string", required: true}
      - {label: "تعداد سکه", name: "coins", widget: "number", default: 1000}
      - {label: "قیمت (تومان)", name: "price", widget: "number", default: 50000}
      - label: "وضعیت"
        name: "status"
        widget: "select"
        options: ["فعال", "غیرفعال"]
        default: "فعال"
      - {label: "توضیحات", name: "description", widget: "text", required: false}